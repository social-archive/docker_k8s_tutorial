apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: todo-app
  labels:
    app: postgres
spec:
  selector:
    app: postgres           # postgres Deployment Pod와 연결
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP           # 클러스터 내부에서만 접근 (앱→DB 전용)
