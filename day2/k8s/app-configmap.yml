apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: todo-app
data:
  # 민감하지 않은 설정값 (DB 호스트, 포트, DB 이름)
  DB_HOST: "postgres"        # Service 이름으로 DNS 조회
  DB_PORT: "5432"
  DB_NAME: "tododb"
